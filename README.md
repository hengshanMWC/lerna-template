# 简介

lerna+yarn 的 monorepo 模版

# lerna

> 基于 GIT(主要是 commit) 和 NPM(工具链) 来辅助和优化开发者管理多包。

通过 git commit 提交记录对比获知更改，针对更新过的包进行批量的更新发版

相关命令

```
lerna publish
lerna version
```

## 具体命令

```js
// 初始化目录
lerna init
//
```
